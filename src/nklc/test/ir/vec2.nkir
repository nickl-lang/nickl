type Vec2 :{[2]f64}

local proc make_vec2(:f64 %x, :f64 %y) :Vec2 %vec {
    offset :Vec2 %vec, 0 -> %x_addr
    store %x -> %x_addr
    offset :Vec2 %vec, 1 -> %y_addr
    store %y -> %y_addr
    ret
}
