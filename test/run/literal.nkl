import std;

ivec2 :: #type (i64, i64);
vec := ivec2{4, 5};
std.printf("%lli %lli\n", vec[0], vec[1]);

Color :: [4]u8; // rgba
red: Color = Color{255, 0, 0, 255};

printColor :: (c: Color) {
    std.printf("[r=%hhu g=%hhu b=%hhu a=%hhu]\n", c[0], c[1], c[2], c[3]);
}

printColor(red);
