{
    a := 256;
    b := cast(u8) a;
    @assert(b == cast(u8) 0);
    @println("a={}, {}{a}={}", a, @typeof(b), b);
}

{
    a := 255;
    b := cast(i8) a;
    @assert(b == cast(i8) -1);
    @println("a={}, {}{a}={}", a, @typeof(b), b);
}
