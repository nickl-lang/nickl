#foreign("")
read :: (fd: i32, buf: *u8, size: u64) -> i64;
#foreign("")
open :: (pathname: *u8, flags: i32, mode: *u8) -> i32;

BUF_SIZE :: 64;
buf: [BUF_SIZE]u8;

fd := open("file.txt", 0, "r");
res := read(fd, &buf, BUF_SIZE);

import std;
std.printf("%s", &buf);
