main :: (argc: i32, argv: **i8) -> i32 {
    return 0;
}

import compiler;
builder := compiler.createBuilder();
compiler.build(builder, cast(compiler.EntryPoint) main, "simple");
compiler.freeBuilder(builder);
