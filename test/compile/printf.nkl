import std;
import compiler;

main :: (argc: i32, argv: **i8) -> i32 {
    std.printf("hello %s\n", "world");
    return i32{0};
}

builder := compiler.createBuilder();
compiler.build(builder, cast(compiler.EntryPoint) main, "printf");
compiler.freeBuilder(builder);
